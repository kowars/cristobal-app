rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /statuses/{statusId} {
      allow read: if true;
      allow write: if false; // Keep false for now, relying on client-side protection
    }
  }
}